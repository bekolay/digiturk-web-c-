@model IEnumerable<digiturkveri.Site_Ayar>

@{
    ViewBag.Title = "Site Ayar";
}

@if (Request.IsAuthenticated && HttpContext.Current.User.IsInRole
("Admin"))
{

    <section id="blog-section">

        <div class="content-wrapper">

            <div class="content-dimmer"></div>
            <header class="page-header">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-xs-12">
                            <br />
                            <h3 class="page-header-heading"><span class="typcn typcn-th-small-outline page-header-heading-icon"></span>Site ayar</h3>
                            <hr />
                        </div>
                    </div>
                </div>
            </header>
            <div class="container" style="background: #ffffff;border-top-width: 50px;padding-top: 50px;padding-bottom: 50px;">
                <table id="datable_1" class="table table-hover display">
                    <thead>
                        <tr>
                            <th>Site Adı</th>
                            <th>Site Tel</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.siteadi)</td>
                                <td>@Html.DisplayFor(modelItem => item.sitetel)</td>
                                <td>
                                    @Html.ActionLink("Düzenle", "Edit", new { id = item.ayarid })
                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            @*<header class="page-header">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-xs-12">
                                <h1 class="page-header-heading"><span class="typcn typcn-th-small-outline page-header-heading-icon"></span>Aktif Olmayan Bloglar</h1>
                            </div>
                        </div>
                    </div>
                </header>
                <div class="container" style="background: #ffffff;border-top-width: 50px;padding-top: 50px;padding-bottom: 50px;">
                    <table id="datable_2" class="table table-hover display  pb-30">
                        <thead>
                            <tr>
                                <th>Blog Adı</th>
                                <th>Tarih</th>
                                <th>Gösterim Sayısı</th>
                                <td></td>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var asd in ViewBag.Aktifblog)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => asd.padi)</td>
                                    <td>@Html.DisplayFor(modelItem => asd.ptarih)</td>
                                    <td>@Html.DisplayFor(modelItem => asd.paktif)</td>
                                    <td>
                                        @Html.ActionLink("Düzenle", "BlokEdit", new { id = asd.pid })
                                    </td>

                                </tr>
                            }
                        </tbody>
                    </table>
                </div>*@
        </div>
    </section>
}
